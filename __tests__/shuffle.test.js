const shuffle = require("../src/shuffle");

describe("shuffle should...", () => {
  test('Checks that shuffle returns an array', () => {
    let result = shuffle()
    expect(result).toBe = Array
  })
  test('Checks to ensure everything has been shuffled', () => {
    let result = shuffle()
    expect(result).toBe = shuffle()
  })
});
